const routerConfig = [
  {
    path: '/',
    component: '../layouts/BasicLayout',
    routes: [
      { path: '/', component: './Home' },
      // ##replace##
    ],
  },
];

export default routerConfig;
